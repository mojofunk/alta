#ifndef MOJO_EXTERNAL_TYPES
#define MOJO_EXTERNAL_TYPES

/*
 * These are the opaque types that are accessible by the clients
 */
namespace mojo {

typedef struct _project project_t;
typedef struct _track track_t;

};

#endif
