/*
 * The canvas items should not handle any events themselves but rather defer
 * events
 * to an event handler. Each Canvas will have a single event handler that
 * processes
 * the events or defers processing to other classes.
 */
class CanvasEventHandler {
};
