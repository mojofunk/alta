#! /usr/bin/env python
# encoding: utf-8

subdirs = '''
cppformat
pugixml
moodycamel
'''


def configure(conf):
    for dir in subdirs.split():
        conf.recurse(dir)


def build(bld):
    bld.recurse(subdirs)
